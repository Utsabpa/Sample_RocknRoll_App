
import { eachData } from 'rock-and-roll-ui/helpers/each-data';
import { module, test } from 'qunit';

module('Unit | Helper | each data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eachData([42]);
  assert.ok(result);
});

